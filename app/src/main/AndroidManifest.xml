<?xml version="1.0" encoding="utf-8"?>
<manifest
    xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ayvytr.ktxapp">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

    <application
        android:name=".App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".et.EditTextActivity"></activity>
        <activity android:name=".TextChangeActivity" />
        <activity android:name=".TestRepeatClickActivity" />
        <activity android:name=".activitystack.SecondActivity" />
        <activity android:name=".activitystack.FirstActivity" />
        <activity android:name=".activitystack.StackActivity" />
        <activity android:name=".ext.FiltersActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ResourcesActivity"
            android:configChanges="orientation|keyboardHidden|screenSize"
            android:exported="true" />
        <activity android:name=".LogTestActivity" />
        <activity android:name=".ext.StatusBarActivity" />
        <activity android:name=".rich.RichActivity" />
    </application>

</manifest>